apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  labels:
    app: backend
    app.kubernetes.io/name: backend
    app.kubernetes.io/component: api
data:
  # ConfigMaps are for non-sensitive configuration only; do not store passwords, tokens, or keys here.
  NODE_ENV: production
  LOG_LEVEL: info
  PORT: "8080"
